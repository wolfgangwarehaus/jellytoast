__pycache__/
*.py[cod]
*$py.class
*.so

.venv/
venv/
env/
.env

build/
dist/
*.egg-info/
.eggs/

# libmpv-2.dll is a 112 MB build artifact — copy from the installed app before
# running PyInstaller (see packaging/pyinstaller/jellytoast.spec). Not versioned.
packaging/windows/libmpv/

.pytest_cache/
.mypy_cache/
.ruff_cache/
.pre-commit-cache/

# coverage (pytest-cov)
.coverage
.coverage.*
htmlcov/
coverage.xml

# logs (live-harness scratch — e.g. .live.log — and any stray run logs)
*.log

.vscode/
.idea/
.claude/
# Claude Code project guide — kept local (and in the private ops repo), not public.
/CLAUDE.md
*.swp
*~
.DS_Store
